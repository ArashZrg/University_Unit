#ifndef UNIVERSITY_UNIT_MENU_H
#define UNIVERSITY_UNIT_MENU_H

#include <iostream>
#include <string>

using namespace std;

class Menu {

public:
    static string whileMenu();

    static string sectionMenu();

    static string interferenceMenu();

    static string locationMenu();

    static string teacherMenu();

    static string studentMenu();

};


#endif //UNIVERSITY_UNIT_MENU_H
